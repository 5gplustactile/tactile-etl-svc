apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "airflow-operator.fullname" . }}-test-connection"
  labels:
    {{- include "airflow-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "airflow-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
